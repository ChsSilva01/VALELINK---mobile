import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    NotificationArea: {
        width: '100%',
        height: 65,
        top: 5,
        backgroundColor: '#00C16C',
        opacity: 0.1
    }

})